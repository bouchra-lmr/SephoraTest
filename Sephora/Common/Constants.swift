import UIKit

enum Constants {
    
    static let title = "SHEPHORA"

    enum API {
        static let host = "sephoraios.github.io"
        static let scheme = "https"
    }
    
    enum MIME {
        static let content = "Content-Type"
        static let json = "application/json"
    }
    
    enum Dimension {
        static let cellHeight: CGFloat = 260
        static let horizontalInset: CGFloat = 32
    }
}
